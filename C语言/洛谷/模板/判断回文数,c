#include<stdio.h>
void a(int num) {

    int temp = num;
    int ans = 0;
    while (temp != 0) 
    {
        ans = ans * 10 + temp % 10;
        temp /= 10;
    }
    if (ans == num)
    {
        printf("%d", num);
    }
        
    
}