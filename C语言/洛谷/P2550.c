#include<stdio.h>
int main()
{
    int n, m;
    int num[7];
    int s[7] = {0};
    int l = 7;
    scanf("%d", &n);
    for (int i = 0; i < 7; i++)
    {
        scanf("%d ", &m);
        num[i] = m;
    }
    int r[n][l];
    for (int i = 0; i < n; i++)
    {
        for (int i = 0; i < l; i++)
        {
            
        }
    }
}